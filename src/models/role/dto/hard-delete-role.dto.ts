import { ApiProperty } from '@nestjs/swagger';

export class HardDeleteRoleDto {
  // Hiện tại chưa cần field nào cho việc xóa cứng,
  // nhưng định nghĩa sẵn để đảm bảo tính nhất quán của Project.
}
